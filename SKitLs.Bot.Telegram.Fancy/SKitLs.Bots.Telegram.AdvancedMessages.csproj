<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net6.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <Description>Offers enhanced methods for sending messages in Telegram bots.

An extension project built upon the SKitLs.Bots.Telegram.Core Framework.</Description>
    <Version>2.2.0</Version>
    <FileVersion>$(AssemblyVersion)</FileVersion>
    <PackageReleaseNotes>(!) Since .Core v2.3 - Delivery System critical bug fix

(+) EditWrapper static Task&lt;EditWrapper&gt; FromBuildable Shortcut Added

(~) ReplyMenu .Add() &amp; .AddRange() namings fix
(~) InlineBase no menu bug fix
(~) ReplyBase no menu bug fix</PackageReleaseNotes>
    <Title>$(AssemblyName)</Title>
    <Authors>SKitLs, Sargeras02</Authors>
    <Copyright>Copyright (C) SKitLs 2023</Copyright>
    <PackageProjectUrl>https://github.com/SKitLs-dev/SKitLs.Bots.Telegram.git</PackageProjectUrl>
    <PackageIcon>libIcon_AM.png</PackageIcon>
    <PackageTags>Telegram, Bots, Messaging, Formatting</PackageTags>
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <AssemblyVersion>2.2.0</AssemblyVersion>
    <PackageLicenseFile>LICENSE</PackageLicenseFile>
    <PackageRequireLicenseAcceptance>True</PackageRequireLicenseAcceptance>
    <GenerateDocumentationFile>True</GenerateDocumentationFile>
    <PackageReadmeFile>README.md</PackageReadmeFile>
  </PropertyGroup>

  <ItemGroup>
    <Compile Include=".obsolete\DynamicMessage.cs" />
    <Compile Include=".obsolete\IDynamicMessage.cs" />
    <Compile Include=".obsolete\IFormattableMessage.cs" />
    <Compile Include=".obsolete\InlineButtonPair.cs" />
  </ItemGroup>

  <ItemGroup>
    <None Include="..\..\libIcon_AM.png">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="SKitLs.Bots.Telegram.ArgedInteractions" Version="1.4.0" />
    <PackageReference Include="SKitLs.Bots.Telegram.Core" Version="2.3.0" />
    <PackageReference Include="Telegram.Bot" Version="19.0.0" />
  </ItemGroup>

  <ItemGroup>
    <None Update="LICENSE">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
    <None Update="README.md">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

  <ItemGroup>
    <Folder Include="Model\Messages\Attaches\" />
  </ItemGroup>

</Project>
