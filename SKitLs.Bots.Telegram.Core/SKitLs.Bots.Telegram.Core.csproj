<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net6.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AssemblyVersion>1.5.1</AssemblyVersion>
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <Title>$(AssemblyName)</Title>
    <Authors>Sargeras02, SKit-Labs</Authors>
    <PackageProjectUrl>https://github.com/Sargeras02/SKitLs.Bots.Telegram.git</PackageProjectUrl>
    <PackageIcon>libIcon.png</PackageIcon>
    <PackageTags>Telegram, Bots, Management</PackageTags>
    <FileVersion>$(AssemblyVersion)</FileVersion>
    <PackageReleaseNotes>[1.5.0] + [1.5.1]

( ! ) Loggers (*.external namespace) were moved to stand-alone library.
See: SKitLs.Utils.

(*) Debug Language bug fixed.

(+) README Added. README Updated.

(~) Descriptions and naming updated.</PackageReleaseNotes>
    <PackageLicenseFile>LICENSE</PackageLicenseFile>
    <PackageRequireLicenseAcceptance>False</PackageRequireLicenseAcceptance>
    <Copyright>Copyright (C) Sargeras02 2023</Copyright>
    <Description>The core module of the SKitLs.Bots.Telegram Framework.</Description>
    <Version>$(AssemblyVersion)</Version>
    <PackageReadmeFile>README.md</PackageReadmeFile>
  </PropertyGroup>

  <ItemGroup>
    <Compile Remove="Settings\**" />
    <EmbeddedResource Remove="Settings\**" />
    <None Remove="Settings\**" />
  </ItemGroup>

  <ItemGroup>
    <None Remove="resources\locals\en.core.json" />
    <None Remove="resources\locals\ru.core.json" />
  </ItemGroup>

  <ItemGroup>
    <None Include="..\..\libIcon.png">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

  <ItemGroup>
    <Resource Include="resources\locals\en.core.json" />
    <Resource Include="resources\locals\ru.core.json" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="SKitLs.Utils.LocalLoggers" Version="1.2.7.1" />
    <PackageReference Include="Telegram.Bot" Version="19.0.0" />
  </ItemGroup>

  <ItemGroup>
    <None Update="LICENSE">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
    <None Update="README.md">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

</Project>
